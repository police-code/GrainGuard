document.getElementById("scan-button").addEventListener("click", function() {
    // Optionally trigger the camera here, or directly redirect to AI simulation page
    window.location.href = "ai-simulation.html";
});
